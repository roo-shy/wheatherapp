var React = require('react');
var Header = require('../components/Header');

var HelloWorld = React.createClass({
  render: function () {
    return (
      <div> Hello World </div>
    )
  }
});

module.exports = Main;
